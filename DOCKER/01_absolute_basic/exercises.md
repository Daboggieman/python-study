# Docker Level 1 Exercises

These exercises reinforce the very first Docker concepts.

1. Run the command `docker run hello-world` and write down what it does.
2. Use `docker version` and `docker info` to view the Docker installation details.
3. Explain in one sentence the difference between an image and a container.
4. List three basic Docker commands that show or remove containers and images.
