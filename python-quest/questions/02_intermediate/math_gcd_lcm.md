# Math Gcd Lcm

Source: math_gcd_lcm

Files to submit
- solution.py

Allowed functions
- --allow-builtin

Instructions
Using `math.gcd`, write a function that returns both the greatest common divisor and least common multiple of two positive integers as a tuple `(gcd, lcm)`. Recall that `lcm(a, b) = a * b // gcd(a, b)`.

Expected function
```python
import math

def gcd_lcm(a, b):
    pass
```


Here is a possible program to test your function :
```python
print(gcd_lcm(48, 18))
print(gcd_lcm(7, 5))
```

And its output :
```text
(6, 144)
(1, 35)
```
