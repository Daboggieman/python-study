# Math Is Prime

Source: math_is_prime

Files to submit
- solution.py

Allowed functions
- --allow-builtin

Instructions
Using `math.sqrt`, write a function that efficiently checks whether `n` is prime (only test divisors up to the square root of `n`).

Expected function
```python
import math

def is_prime(n):
    pass
```


Here is a possible program to test your function :
```python
print(is_prime(17))
print(is_prime(18))
print(is_prime(2))
print(is_prime(1))
```

And its output :
```text
True
False
True
False
```
