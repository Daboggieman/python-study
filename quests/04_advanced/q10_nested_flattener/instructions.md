# Nested Flattener

Write a function named `flatten(nested)` that turns a nested list into a single flat list.

## Requirements
- Accept lists that may contain other lists.
- Flatten them one level only.
- Preserve the order of elements.

## Example
```python
flatten([1, [2, 3], 4])
# -> [1, 2, 3, 4]
```
