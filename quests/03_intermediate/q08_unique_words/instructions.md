# Unique Words

Write a function named `unique_words(text)` that returns a sorted list of all unique words in a string.

## Requirements
- Ignore punctuation and case.
- Return the words in alphabetical order.

## Example
```python
unique_words("One two two three, three four")
# -> ["four", "one", "three", "two"]
```
