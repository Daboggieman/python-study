# Linear Search Basic

Source: linear_search_basic

Files to submit
- solution.py

Allowed functions
- --allow-builtin

Instructions
Write a function that performs a linear search for `target` in `arr` and returns its index, or `-1` if not found. Do not use `.index()` or `in`.

Expected function
```python
def linear_search(arr, target):
    pass
```


Here is a possible program to test your function :
```python
print(linear_search([4, 2, 9, 7], 9))
print(linear_search([4, 2, 9, 7], 100))
```

And its output :
```text
2
-1
```
