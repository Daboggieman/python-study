# Hash Two Sum

Source: hash_two_sum

Files to submit
- solution.py

Allowed functions
- --allow-builtin

Instructions
Write a function that returns a tuple of the two INDICES of the numbers in `nums` that add up to `target`, running in O(n) time using a dictionary (not the O(n^2) nested-loop approach). Assume exactly one solution exists.

Expected function
```python
def two_sum(nums, target):
    pass
```


Here is a possible program to test your function :
```python
print(two_sum([2, 7, 11, 15], 9))
print(two_sum([3, 2, 4], 6))
```

And its output :
```text
(0, 1)
(1, 2)
```
